h1 = @site.name

table.table
  thead
    - @site.product_options.each do |option|
      th = option.name.humanize
  tbody
    - @products.each do |product|
      tr
        - @site.product_options.each do |option|
          td = raw product.option(option).parsed_value

= paginate @products

p = link_to 'Import products to parse', new_product_path
